# Astro migration notes

This branch converts the current static site into an Astro static site with real language URLs:

- /en/
- /es/
- /nl/
- /ca/

Cloudflare Pages settings for this branch/future launch:

- Framework preset: Astro
- Build command: npm run build
- Build output directory: dist
- Root directory: leave empty

The live site should not be switched to these settings until the branch preview is approved.
